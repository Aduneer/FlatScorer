# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Environments & Dependencies
.venv/
venv/
env/
ENV/
build/
dist/
*.egg-info/

# Generated FlatScorer Output Files
output/

# User Configuration Overrides
# Keep default config templates tracked, but ignore custom local configs with secrets or personal addresses
config.json
!config.example.json

# Spatial / OSMnx Cache Directories
# The osmnx cache now lives in the per-user cache dir, not here; these two stay
# so a checkout that still has one from an older version doesn't get committed.
.cache/
cache/

# Personal working notes / idea backlog
TODO.md

# IDEs and Editors
.vscode/
.idea/
*.swp
*.swo
.DS_Store

